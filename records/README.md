# Records

Results and audits, each written against a pre-registration in `../prereg/` and timestamped when it
was written. Verify any of them with the recipe in the top-level README.

| file | what it records |
|---|---|
| `E3_RESULT.md` | the causal threshold-control test of Section 7. Outcome C: the adaptive rule loses to the fixed one on net, Sharpe, drawdown, break-even, and on the diagnostic it was built to improve |
| `DETACHED_RESULT.md` | the detached-denominator falsification of Figure 3, testing whether ω inflating its own denominator produces the effect |
| `LOO_RESULT.md` | the leave-one-leg-out test of the same objection. 2.7% of legs change denominator; the compression slope flattens 17.8% and threshold invariance strengthens |
| `HK_RESULT.md` | the Hong Kong external replication, reported in two layers. H1 and H3 pass on the never-inspected period; H2 fails on significance and the H4 ordering fails |
| `INTEGRITY_partial.md` | the acquisition and integrity check on the Hong Kong feed, and the `--partial` dry run that found a display fault before the real execution |
| `PEEK_2026-08-31.md` | the disclosure that Level A had been grouped by calendar date, which is invalid for a window crossing midnight, and the amendment frozen before the code was changed |
| `AUDIT_2026-08-30.md` | a full audit of the manuscript. A label collision that silently changed the walk-forward candidate set; figures built from the specification window while the tables used the full sample; several statements corrected |
| `AUDIT_2026-08-31_correction.md` | withdraws a conclusion from the previous audit that was itself wrong |
| `E4_PARKED.md` | a hypothesis generated by the E3 result, registered and deliberately not tested on this dataset |
| `FORWARD_SPEC.md` | the frozen forward paper-trading specification |

All ten records carry Bitcoin block attestations. `E4_PARKED.md.ots` and
`FORWARD_SPEC.md.ots` were the last to confirm, on 2026-09-01.
